{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host & npx tailwindcss -i ./css/style.css -o ./src/style.css --watch",
    "build": "NODE_ENV=production npx tailwindcss -i ./css/style.css -o ./src/style.css --minify && tsc && vite build",
    "serve": "vite preview --host"
  },
  "devDependencies": {
    "@types/three": "^0.129.1",
    "autoprefixer": "^10.2.6",
    "postcss": "^8.3.5",
    "tailwindcss": "^2.2.4",
    "three": "^0.129.0",
    "typescript": "^4.3.2",
    "vite": "^2.3.8"
  }
}
